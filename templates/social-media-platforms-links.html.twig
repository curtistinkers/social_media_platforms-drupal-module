{#
/**
 * @file
 * Template to display ain social media icon with a link.
 *
 * Available variables:
 * - attributes: HTML attributes for the item.
 * - links: A nested list of link items. Each link item contains:
 *   - attributes: HTML attributes for the menu item.
 *   - image_url: The social media link image path.
 *   - title: The link title.
 *   - link_url: The link URL, instance of \Drupal\Core\Url
 */
#}

{% set link_classes = ['social-media-platforms__link '] %}
{% set container_classes = ['social-media-platforms__container '] %}

<div{{ attributes.addClass(container_classes) }} {{ attributes|without('class') }}>
    {% for key, link in links %}
        {% if link.link_url|render|trim is not empty  %}
          {% set link_classes = link_classes|merge(['social-media-platforms__link--' ~ key]) %}
          <a{{ link.attributes.addClass(link_classes) }} href="{{ link.link_url }}" {{ target_blank ? 'target="_blank"' : '' }} {{ attributes|without('class') }} >
            {% if show_icon  %}
              <img class="social-media-platforms__icon" src="{{ link.image_url }}" width=30 />
            {% endif %}
            {% if show_label  %}
              {{ link.title }}
            {% endif %}
          </a>
        {% endif %}
    {% endfor %}
</div>
